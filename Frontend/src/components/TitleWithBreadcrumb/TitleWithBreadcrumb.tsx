import TitleWithBreadcrumbComponent, {
  TitleWithBreadcrumbProps,
} from 'core/molecules/TitleWithBreadcrumb';

const TitleWithBreadcrumb = (props: TitleWithBreadcrumbProps): JSX.Element => {
  return <TitleWithBreadcrumbComponent {...props} />;
};

export default TitleWithBreadcrumb;
