import CheckoutFooterComponent, { CheckoutFooterProps } from 'core/molecules/CheckoutFooter';

const CheckoutFooter = (props: CheckoutFooterProps): JSX.Element => (
  <CheckoutFooterComponent {...props} />
);

export default CheckoutFooter;
