import CategoryBadgesComponent, { CategoryBadgesProps } from 'core/molecules/CategoryBadges/';

const CategoryBadges = (props: CategoryBadgesProps): JSX.Element => {
  return <CategoryBadgesComponent {...props} />;
};

export default CategoryBadges;
