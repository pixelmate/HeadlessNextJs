import ForgotPasswordComponent, { ForgotPasswordProps } from 'core/molecules/ForgotPassword';

const ForgotPassword = (props: ForgotPasswordProps): JSX.Element => {
  return <ForgotPasswordComponent {...props} />;
};

export default ForgotPassword;
